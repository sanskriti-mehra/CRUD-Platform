Create files/folders as above.

npm init -y

npm install express ejs body-parser method-override uuid fs-extra

node index.js

Open http://localhost:3000/?user=alice to see Admin UI as Admin (alice). Or switch to ?user=mike or ?user=victor.

